# Metric Versioning

Every metric is immutable and versioned.

Example:

health_score_v1
health_score_v2

Stored metadata:

- formula
- dependencies
- created_at
- author
- confidence

Historical comparisons remain reproducible.
