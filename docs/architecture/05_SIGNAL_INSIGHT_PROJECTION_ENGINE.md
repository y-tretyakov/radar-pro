# Signal, Insight and Projection Engine v2

Radar Pro must provide explanations.

Instead of:

```
Score: 91
```

Provide:

```
Why:
+32 contributor growth
+24 release velocity
+18 community activity
-10 maintainer concentration
```

Layers:

Signal Engine → detects changes
Insight Engine → explains causes
Projection Engine → predicts possible trends

Future ML models consume the same signals without changing the platform API.
